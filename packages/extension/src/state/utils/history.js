/**
 * Custom History for Routes
 */

import { createBrowserHistory } from 'history';

// creating a user-defined history object for browser navigation
// without relying on BrowserRoute
export default createBrowserHistory();
